# requirements.txt
# Dependencias para "Seguimiento y Control de Rutas"

# Interfaz gráfica (PyQt6)
PyQt6
PyQt6-WebEngine

# Llamadas HTTP a OpenStreetMap (Nominatim + OSRM)
requests

# Generación de reportes en PDF
reportlab

# Mapas interactivos (para Folium en el visor de mapas)
folium

# (Opcional) Para manejo avanzado de fechas o utilidades adicionales
# No se usa actualmente, pero folium lo requiere como dependencia implícita